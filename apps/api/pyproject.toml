[project]
name = "api"
version = "0.1.0"
description = "Web Backend Gateway for Awaqi"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110.0",
    "pydantic>=2.7.0",
    "python-multipart>=0.0.9",
    "uvicorn>=0.29.0",
    "apscheduler>=3.10.0",
    "rq>=1.16.0",
    "redis>=5.0.0",
    "ai-engine",
    "database",
]

[tool.uv.sources]
ai-engine = { workspace = true }
database = { workspace = true }
