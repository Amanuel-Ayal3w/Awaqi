[project]
name = "database"
version = "0.1.0"
description = "Shared database models and schemas"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy>=2.0",
    "pgvector",
    "alembic",
    "asyncpg",
    "psycopg2-binary",
    "bcrypt>=4.2.0",
    "redis",
    "python-dotenv",
    "utils",
]

[tool.uv.sources]
utils = { workspace = true }
