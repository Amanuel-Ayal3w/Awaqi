[project]
name = "nlu"
version = "0.1.0"
description = "Natural Language Understanding utilities"
requires-python = ">=3.11"
dependencies = [
    "utils"
]

[tool.uv.sources]
utils = { path = "../utils", editable = true }
